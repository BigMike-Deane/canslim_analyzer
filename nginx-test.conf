server {
    listen 8002;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://canslim-analyzer:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 300s;
    }

    location /health {
        proxy_pass http://canslim-analyzer:8001;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# DEPENDENCY POLICY (2026-09-09): EXACT PINS ONLY.
#
# Everything here was ">=" until today, so every `docker-compose build`
# silently pulled the newest release of each package onto a live trading
# system. The drift found when this was finally measured against the running
# container was not small:
#
#   yfinance                 >=0.2.40 -> 1.7.0   (0.x to 1.x, MAJOR)
#   redis                    >=5.0.0  -> 8.1.0   (three majors)
#   xgboost                  >=2.0.0  -> 3.2.0   (MAJOR)
#   pandas-market-calendars  >=4.4.0  -> 5.4.0   (MAJOR)
#   scikit-learn             >=1.3.0  -> 1.9.0
#   slowapi                  >=0.1.9  -> 0.1.10  (moved TODAY, mid-session)
#
# yfinance fetches every price this system trades on, and it was a full
# major version beyond what was ever validated. Nothing broke -- but nothing
# checked, either, and a scoring or exit decision made on subtly different
# price data is exactly the kind of failure this program cannot detect from
# the inside.
#
# Versions below are the set VERIFIED RUNNING in production on 2026-09-09,
# with the suite green and live trading healthy. Upgrades are now a
# deliberate act: bump one line, run the suite, deploy, watch.
#
# Refresh the pins from what is actually deployed:
#   docker exec canslim-analyzer python -m pip freeze
#
# SECURITY FLOORS -- do NOT pin below these (Aug-20 dependency sweep, CVE
# clearing): cryptography>=50.0.0, idna>=3.15, pyasn1>=0.6.4,
# soupsieve>=2.8.4, aiohttp>=3.14.3, python-multipart>=0.0.31.
# Exact pins mean security patches no longer arrive on their own: that is
# the deliberate trade, and it is why the periodic dep sweep still matters.

# fastapi/starlette pinned to the pair validated by the full test suite
# (aug-20 security dependency sweep — clears all starlette PYSEC advisories)
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
python-dotenv==1.2.2
yfinance==1.7.0
pandas==2.1.3
# NYSE session calendar. Replaces a hardcoded 2026-2027 holiday table that
# would have reported every 2028 holiday as a trading day (see
# backend/ai_trader.py _nyse_holidays_for_year). Rules are computed, not
# tabulated, so no annual maintenance and no network access at import.
# NOTE: the Dockerfile installs THIS file, not the root requirements.txt.
pandas-market-calendars==5.4.0
numpy==1.26.2
requests==2.33.1
apscheduler==3.10.4
pyyaml==6.0.3
redis==8.1.0
pytest==9.1.1
pytest-cov==7.1.0
aiohttp==3.14.3
psycopg2-binary==2.9.12
python-multipart==0.0.32
python-jose[cryptography]==3.5.0
# Security floors for transitive deps flagged by pip-audit (aug-20 sweep)
cryptography==50.0.1
idna==3.19
pyasn1==0.6.4
soupsieve==2.9.2
passlib[bcrypt]==1.7.4
google-auth==2.57.1
slowapi==0.1.10
scikit-learn==1.9.0
xgboost==3.2.0
joblib==1.6.0
pywebpush==2.5.0
